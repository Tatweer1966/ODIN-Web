# Milestone 1 acceptance criteria

The milestone is accepted only when all commands pass:

```text
npm run typecheck
npm run lint
npm test
npm run build
```

No AST, codemod, localization synchronization, or military validation code is included in this milestone.
